# 겹 — 기술 아티클 생산 설계 (백엔드 / iOS)

목적: 겹 프로젝트에서 **우아한형제들 Log Bat 아티클 수준의 기술 글**이 나오게 하는 것. 그리고 그 과정이 **당근 로컬잡스(AI 에이전트) 지원 조건**과도 맞물리게 하는 것.

핵심 전제: **좋은 아티클은 글쓰기가 아니라 설계에서 나온다.** Log Bat 팀이 좋은 글을 쓸 수 있었던 건 문장력이 아니라, 부하 테스트 환경을 먼저 만들고 수치를 남겼기 때문이다. 측정하지 않은 것은 쓸 수 없다. 그러므로 **측정 포인트를 개발 전에 코드에 심는다.**

---

## 1. Log Bat 아티클의 해부 — 우리가 복제할 골격

| 구간 | Log Bat이 한 것 | 우리가 복제할 것 |
|---|---|---|
| 주제 선정 | "프로젝트하며 로그를 매번 전달받는 게 불편했다" — 자기가 겪은 문제 | 겹의 기능이 아니라 **겪은 병목**에서 출발 |
| MVP 1 | 2일 안에 최소 기능. **기술 선택에 근거**("영속성이 필요한가? No → JPA 대신 JDBC") | 대안을 검토하고 탈락시킨 이유를 남긴다 |
| 테스트 환경 | Lambda 400개 동시 실행. "평균 처리량뿐 아니라 **어디서 깨지는지**가 포인트" | 가상 러너 부하 스크립트를 D2에 미리 만든다 |
| 병목 발견 | DB Connection 풀 고갈 — **수치와 함께** | 실패 지점을 수치로 기록 |
| MVP 2 | Bulk Insert + 캐시 → TPS 400 → **864**, 응답 419ms | before/after 수치가 글의 척추 |
| MVP 3 | **가설 1 (DB 병목?) → Scale-Up 해봄 → 아님 → 가설 2 (자원 경쟁?) → LinkedBlockingQueue의 `addAll()` lock 경합 발견** | 이 "틀린 가설을 먼저 쓰는" 구조가 글의 신뢰도를 만든다 |
| 실패 기록 | 멱등성 깨진 이유 3가지(데브서버 미분리, VPC 공유, RDS 크레딧)를 그대로 공개 | 사고를 숨기지 않는다 |
| 마치며 | 못 한 것과 다음 계획을 솔직하게 | To be continued |

**복제 불가능한 것 하나:** Log Bat은 "초당 로그 15,000개" 같은 대용량이 자연 발생하는 도메인이다. 겹은 러너 150명 규모라 그런 트래픽이 없다. 그러므로 **부하는 시뮬레이션으로 만들되, "아카데미 전 기수·전 캠퍼스로 확장하면"이라는 가정을 명시**한다. 없는 트래픽을 있는 척하지 않고, 확장 시나리오를 설계 근거로 쓰는 것이 정직하고 더 강하다.

## 2. 겹에서 실제로 병목이 나는 지점 (= 아티클 소재)

기능을 나열한 게 아니라, **측정하면 반드시 깨질 곳**을 골랐다.

### 백엔드

**B-1. 신호 fan-out과 Live Activity 갱신 폭풍** ★1순위
- 신호 1개 생성 → 관심사 매칭된 N명에게 푸시. 조인 1건마다 → 참여자 전원 Live Activity 갱신 푸시
- 4명 신호에 4명이 순차 조인하면 갱신 푸시가 1+2+3+4 = 10회. 인원이 늘면 O(n²)
- 순차 APNs 호출 + Cloud Functions 콜드 스타트가 겹치면 "조인했는데 잠금화면이 3초 뒤에 바뀜"
- **개선 방향:** APNs 배치 전송, 500ms 윈도우 debounce로 갱신 병합, 관심사 매칭 인덱스 설계
- **측정:** fan-out p50/p95 지연, 푸시 도달률, 갱신 병합률, Functions 실행시간·콜드스타트 비율

**B-2. 오프라인 교환의 멱등성 — 같은 만남이 2번 기록되는 문제** ★분산 시스템 소재로 최고
- 카드 맞대기는 **오프라인에서 P2P로 발생**한다. 두 기기가 각자 서버에 업로드하면 같은 만남이 2건이 된다
- 재시도·중복 전송·시계 오차까지 겹치면 겹 카운트가 부풀어 오른다 → 지표 신뢰 붕괴
- **개선 방향:** 결정적 `gyeopID` = 정렬된 두 러너 ID + 타임슬롯(5분 버킷) 해시 → 양쪽이 독립적으로 같은 ID 생성 → 서버에서 자연 dedupe. 클라이언트 시계를 못 믿으므로 서버 시각 보정
- **측정:** 중복률(목표 0%), 네트워크 단절 시나리오별 동기화 성공률, 시계 오차 ±N분 내성

**B-3. TTL 소멸의 정확성 — "이미 끝난 신호가 피드에 남아있다"**
- 서버 크론 주기(1분)와 클라이언트 타이머의 불일치. 휘발성이 제품의 핵심 가치라 정확도가 곧 신뢰
- **개선:** 서버 TTL + 클라이언트 필터 이중화, 리스너 쿼리에 시간 조건 포함
- **측정:** 만료 후 노출된 신호 건수, 소멸 지연 분포

### iOS

**I-1. 카드 맞대기 3초 달성기** ★1순위
- 목표: 맞댐 → 교환 완료 3초. 순진하게 구현하면 발견 2s + 세션 수립 1.5s + 전송 0.5s로 4초를 넘는다
- **가설 검증 구조:** 가설 1 "전송이 느리다" → 카드 데이터 압축 → 변화 없음 → 가설 2 "발견이 느리다" → 앱 포그라운드 진입 시 상시 advertising으로 예열 → 개선. Log Bat의 가설 구조를 그대로
- **측정:** OSLog signpost로 발견/연결/전송 3구간 분리 측정, p50/p95, 성공률, 재시도율

**I-2. 셰이더 카드 목록의 프레임 드랍**
- 제너러티브 카드는 예쁘지만 GPU를 먹는다. 받은 카드 50장 목록에서 스크롤하면 프레임이 무너진다
- **개선:** 오프스크린 셰이더 비활성화, 스냅샷 캐싱(`ImageRenderer`), 저해상도 프록시
- **측정:** Instruments GPU 시간, 프레임 타임 분포, 스크롤 중 최저 fps

**I-3. 겹 그래프 150노드 60fps**
- force-directed 시뮬레이션을 메인 액터에서 돌리면 UI가 멈춘다
- **개선:** 계산을 별도 actor로 분리(30Hz), 렌더는 TimelineView로 60fps 보간
- **측정:** 메인 스레드 점유율, hitch 비율, 노드 수별 스케일링

## 3. 개발 중에 미리 심을 것 (이게 이 문서의 핵심)

측정 장치가 없으면 나중에 글을 못 쓴다. 아래는 **기능 코드와 함께 작성하는 계측 코드**다.

| 시점 | 심을 것 |
|---|---|
| D1 | `OSLog` Logger 카테고리 분리(`nearby`, `signal`, `sync`, `render`), signpost 헬퍼 |
| D2 | 맞대기 3구간 signpost(발견/연결/전송) + 성공·실패·재시도 카운터. **첫 실측치를 기록으로 남긴다** |
| D3 | 교환 이벤트에 결정적 `gyeopID` 생성 로직 + 중복 검출 카운터 |
| D4 | Cloud Functions 실행시간·콜드스타트 로깅, APNs 응답 코드 집계, fan-out 대상 수 기록 |
| D4 | **부하 스크립트**: 가상 러너 N명이 신호 생성·조인하는 스크립트(Node). "전 기수 1,000명 확장" 시나리오용 |
| D5 | 지표 대시보드(간단한 Firestore 집계 뷰) — 매일 숫자를 보는 습관이 v0.2 개선의 근거 |

**측정 기록 규칙:** 개선 전 수치를 반드시 커밋 메시지나 `docs/metrics/` 에 남긴다. before가 없으면 after는 자랑이 안 된다.

## 4. 아티클 6편 계획

| # | 제목(가제) | 축 | 언제 쓰나 | 핵심 수치 |
|---|---|---|---|---|
| 1 | 아이폰을 맞대는 3초를 만들기 위해 — MPC 교환 지연 분해기 | iOS | v0.1 출시 직후 | 4.2s → 2.x s, 구간별 분해 |
| 2 | 같은 만남이 두 번 기록되는 문제 — 오프라인 P2P의 멱등성 설계 | 백엔드/분산 | v0.1~v0.2 | 중복률 N% → 0% |
| 3 | 조인할 때마다 푸시가 폭발했다 — Live Activity 갱신 병합기 | 백엔드 | v0.2 | 푸시 10회 → 3회, p95 지연 |
| 4 | 예쁜 카드의 대가 — 셰이더 목록 60fps 되찾기 | iOS | v0.2 | 최저 fps, GPU 시간 |
| 5 | 5일 만에 앱스토어에 올리기 위해 버린 것들 | 프로세스 | 출시 직후 | 컷라인 의사결정 기록 |
| 6 | 150명이 쓰는 앱의 지표를 매일 읽어주는 에이전트 | AI/운영 | v0.2~v0.3 | 제안 적중률 |

각 편의 골격은 동일하다: **문제 상황 → 최초 구현과 그 근거 → 측정 → 틀린 가설 → 맞은 가설 → 개선 → 재측정 수치 → 남은 과제.**

## 5. 당근 로컬 잡스와의 정렬 — 냉정한 진단

**⚠️ 마감이 8월 9일(일) 23:59다. 오늘은 8월 5일. 겹 D1~D5와 완전히 겹친다.**

그리고 더 중요한 사실: **이 포지션은 iOS도 백엔드도 아니다.** 공고가 원하는 것은 "LLM·MCP·에이전트로 실제 제품이나 업무를 개선한 경험", "룰·스킬·메모리로 축적되는 구조", "Evaluation·Observability·Memory 설계"다. 겹의 UWB나 Live Activity는 이 포지션에서 가산점이 아니다.

그러므로 겹은 **지원의 주인공이 아니라 사례**로 쓴다. 주인공은 "겹을 5일 만에 만들기 위해 내가 설계한 AI 개발 워크플로우"다. 이건 없는 이야기를 지어내는 게 아니라, 지금 실제로 하고 있는 일이다.

### 겹에 실제로 붙일 수 있는 에이전트 — "겹 오퍼레이터"

아티클 6번이자 지원서의 실물 근거. v0.2~v0.3에 붙인다.

| 당근 요구 | 겹 오퍼레이터에서의 구현 |
|---|---|
| **Observability** | Firestore 일별 지표(설치·겹 수·신호 생성/성사율·이탈), 앱 OSLog 원격 수집, 크래시, GitHub Issues(러너 제보)를 하나의 컨텍스트로 수집 |
| **여러 도구 연결** | Firestore + GitHub MCP + 러너 채널(Slack/Discord)을 잇는 워크플로우. 수집 → 분석 → 제안 → 이슈 생성까지 사람 개입 없이 |
| **룰** | "성사율 3일 연속 40% 미만이면 원인 가설 3개와 각각의 검증 방법을 제안", "신규 크래시 발생 시 관련 커밋 후보 지목" |
| **메모리** | 과거 제안 → 실행 여부 → 결과를 저장. 다음 제안 시 참조("지난번 알림 시간 조정 제안은 성사율 12%p 개선") |
| **Evaluation** | 제안의 적중률을 2주 뒤 사후 채점하고, 채점 결과가 룰을 갱신 — 스스로 나아지는 루프 |

### 사전질문 4개와 겹의 연결

1. **"최근 가장 흥미롭게 실험한 AI 기술"** → 에이전트에게 룰·컨텍스트·도구를 설계해준 경험. 겹 개발에서 `CLAUDE.md` 룰과 문서 컨텍스트를 어떻게 구성했더니 무엇이 달라졌는지 (프롬프트가 아니라 룰·컨텍스트 이야기를 하라는 게 공고의 요구다)
2. **"6개월 동안 AI로 만든 가장 큰 변화"** → 겹: 5일 개발 → 앱스토어 출시 → 실사용자. 혼자서는 불가능한 스코프를 에이전트 워크플로우로 달성한 사례. 속도가 아니라 **무엇을 위임하고 무엇을 직접 판단했는지**가 답의 핵심
3. **"데이터 기반으로 제품 문제를 제안하는 에이전트를 어떻게 만들지"** → 겹 오퍼레이터가 그대로 답이다. 설계도가 아니라 **돌아가는 것**을 들고 갈 수 있다면 다른 지원자와 차원이 다르다
4. **"회의 일정을 예약하는 사내 에이전트를 어떻게 만들지"** → **겹의 여가 신호가 정확히 같은 문제**다. 여러 사람의 가용 시간을 모아 만남을 성사시키되, 거절 비용을 없애는 구조. "제가 만든 앱의 핵심 메커니즘이 이 문제입니다"로 시작하는 답변은 흔치 않다

### 8/9까지의 현실적 배분

| 날 | 겹 개발 | 당근 지원 |
|---|---|---|
| D1 화 | 골격·온보딩·카드 | — |
| D2 수 | SwiftData·MPC 교환 | 개발 중 AI 워크플로우 결정을 메모(질문 1·2 재료) |
| D3 목 | 인증·계정삭제·신호 | 겹 오퍼레이터 설계 스케치 1장 (질문 3 재료) |
| D4 금 | 신호 조인·푸시·심사물 | — |
| D5 토 | Live Activity·엣지·**심사 제출** | 저녁: 지원서 초안 4문항 |
| D6 일 | 심사 대응 | **오전~오후: 지원서 완성·제출 (23:59 마감, 최소 3시간 여유)** |

지원서 4문항은 겹 개발 중에 재료가 쌓인다. **따로 시간을 내서 쓰는 게 아니라, 개발하며 결정한 것을 기록해두고 D5~D6에 문장으로 옮기는 것**이 유일하게 가능한 방식이다.

## 6. 지금 결정해야 할 것

1. **당근에 지원할 것인가** — 마감 4일. 지원한다면 D2부터 워크플로우 결정을 기록해야 한다(사후 복원은 부실해진다)
2. **겹 오퍼레이터를 v0.2에 넣을 것인가** — 넣으면 아티클 6편·지원 답변 3번이 실물이 되지만 스코프가 늘어난다. 판단: **지원한다면 넣는다**(설계+프로토타입 수준)
3. **아티클 1·2편을 출시 직후 2주 안에 쓸 것인가** — 측정치는 시간이 지나면 흐려진다. 쓸 거면 D2부터 수치를 남겨야 한다

# Netlify · Cloudflare Pages 헤더 규칙.
# AASA는 확장자가 없어 호스트가 기본으로 octet-stream을 붙이는 경우가 있는데,
# Apple의 AASA CDN은 application/json을 요구한다 — 여기서 명시적으로 지정한다.
# (GitHub Pages는 이 파일을 지원하지 않는다. site/README.md 참고)
/.well-known/apple-app-site-association
  Content-Type: application/json

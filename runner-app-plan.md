# 러너 여가 앱 — 오프라인 제약을 뚫는 실서비스 기획

> **2026-08-04 업데이트:** 이 기획은 팀 CBL 연장선의 초안이다. 이후 **완전 별개의 개인 프로젝트**로 분리하면서 문제 정의를 다시 세우고 기술 제약을 전부 해제한 명세를 새로 작성했다 → **`gyeop-spec.md`가 현행 문서다.** 이 문서는 스택 결정 근거(5장)와 동아리 지원 연결(8장) 참조용으로 유지한다.

워킹 타이틀: **겹(Gyeop)** — 관심사가 "겹치는" 순간을 만들고, 만남이 쌓여 친밀감의 "겹"이 되는 앱.
(대안: 포잇 Poit, 물결 Mulkyul. 이름은 나중에 갈아끼워도 되는 부품이다.)

이 문서는 팀 CBL 결과물(EQ → CS → GQ → CR)을 출발점으로 삼되, 거기서 멈추지 않는다.
목표는 두 가지다.

1. **실제 러너들이 아카데미에서 매일 쓰는 서비스**를 만든다. 포스터·소모임 같은 오프라인 임시방편이 아니라, 온라인으로 돌아가는 진짜 서비스.
2. 그 과정에서 **디프만·넥스터즈·SOPT iOS 서류/면접에서 넉넉히 1인분을 증명할 수 있는 기술적 깊이**를 확보한다. 얕은 기능 10개가 아니라, "왜 이 기술을 썼고 어떻게 동작하는지"를 끝까지 설명할 수 있는 기능 3~4개.

---

## 1. 출발점 — 팀 CBL이 확정한 것

- **Essential Question:** 어떻게 하면 아카데미에 있는 동안 다양한 관심사를 가진 사람들과 포항에서 여가를 보낼 수 있을까?
- **Challenge Statement:** 아카데미에 있는 동안, 다양한 관심사를 가진 사람들과 포항에서 여가를 보낼 수 있도록 도와주자.
- **App Concept Statement (CR):** "애플 디벨로퍼 아카데미 러너들이 본인의 **정체성**을 드러내는 방식을 통해 **여가시간**에 쉽게 **친밀감**을 높일 수 있도록 돕는 앱."
- **리서치 인사이트:**
  - 여가시간 만남은 작위적으로 시작되지 않는다 → 모임 "개설"보다 낮은 문턱의 신호가 필요하다.
  - 러너들 간 친밀감을 쌓고자 하는 니즈가 실재한다.
  - 지자체(포항) 제공 정보는 광고성이 아닌 양질의 데이터다 → 포항 여가 스팟 콘텐츠로 쓸 수 있다.

여기서 지킬 것: **정체성 → 친밀감**이라는 가치 축. 여기서 뚫을 것: 오프라인 제약.

## 2. "오프라인 제약을 뚫는다"의 의미

기존 아이디어의 도달점은 포스터, 소모임 공지 같은 아날로그 매개였다. 그 한계는 명확하다.

| 오프라인 방식의 한계 | 앱이 뚫는 방법 |
|---|---|
| 포스터는 붙인 순간부터 낡는다 (지금 상태를 못 담음) | **실시간 여가 신호** — "지금" 켜고 끄는 상태 |
| 모임 개설은 문턱이 높다 (작위적, 거절 부담) | 모임이 아니라 **신호**를 보낸다. 응답은 선택 |
| 누가 뭘 좋아하는지 마주쳐야만 안다 | **정체성 카드**가 온라인에 상시 존재 |
| 만남이 일회성으로 증발한다 | 만남의 **기록이 쌓이고** 관계 그래프가 남는다 |
| 아카데미 기수가 끝나면 리셋된다 | 데이터와 서비스가 다음 기수로 **이어진다** |

즉 이 앱은 "오프라인 만남을 대체"하는 게 아니라, **오프라인 만남의 발화 지점과 기록을 온라인화**한다. 만남 자체는 여전히 포항의 현실 공간에서 일어난다 — 앱은 그 앞뒤를 맡는다.

## 3. 핵심 루프

```
정체성 카드 (나를 표현)
   → 여가 신호 (지금 ○○ 할 사람?)
      → 겹침 (관심사·시간이 겹치는 사람 매칭)
         → 만남 (포항 스팟에서, 카드 맞대기로 인증)
            → 겹 쌓임 (기록·통계·관계 그래프)
               → 더 두꺼워진 정체성 카드 (다시 처음으로)
```

한 루프가 돌 때마다 카드가 두꺼워지고, 카드가 두꺼울수록 다음 신호의 응답률이 올라간다. 이게 리텐션 엔진이다.

## 4. 기능 명세 — 각 기능은 iOS 기술 쇼케이스이기도 하다

우선순위 순. P1까지가 MVP, P2는 v1, P3는 그 이후.

### F1. 정체성 카드 (P1)

**장면:** 러너가 온보딩에서 관심사·여가 성향(정적/동적, 실내/실외)·한 줄 자기표현을 고르면, 그 조합이 시각적으로 유일한 카드가 생성된다. 관심사 조합이 카드의 색·패턴·질감을 결정한다. 같은 카드는 세상에 없다.

- **iOS 기술:** SwiftUI `MeshGradient`(iOS 18) + `layerEffect` Metal 셰이더로 관심사 시드 기반 생성 비주얼. `matchedGeometryEffect`로 목록↔상세 전환. 카드 모델은 `struct`(값 타입) — 불변 스냅샷으로 다루고, 변경은 새 값 생성.
- **왜 이 기술:** "정체성을 드러내는 방식"이 이 앱의 CR 그 자체다. 카드가 예쁘고 유일해야 교환하고 싶어진다. 셰이더·메시그라디언트는 데모 첫 10초에 시선을 잡는 화면이자, 면접에서 SwiftUI 렌더링 파이프라인을 이야기할 소재다.
- **면접 연결:** Class vs Struct(왜 카드가 값 타입인가), SwiftUI 렌더링/애니메이션 원리.

### F2. 카드 맞대기 — NameDrop식 근접 교환 (P1)

**장면:** 복도에서 처음 대화한 두 러너가 아이폰 윗부분을 서로 가까이 대면, NameDrop처럼 서로의 정체성 카드가 물결 애니메이션과 함께 교환된다. 교환된 카드는 "만난 사람" 목록에 쌓이고, 겹치는 관심사가 하이라이트된다.

- **iOS 기술:** **NearbyInteraction(UWB)** 으로 거리·방향 측정 + **MultipeerConnectivity**로 피어 발견과 카드 데이터 전송. UWB 미지원 기기는 MPC 근접 발견만으로 폴백. 교환 순간 햅틱(`CoreHaptics`) + 사운드.
- **왜 이 기술:** 아카데미는 전원이 아이폰을 들고 같은 건물에 있는, 지구상에서 UWB 근접 기능이 가장 잘 작동할 환경이다. 서버 없이 P2P로 동작하므로 네트워크가 죽어도 교환은 된다(진짜 의미의 오프라인 제약 돌파). 그리고 이 기능은 다른 지원자가 흉내 내기 어려운 차별화 포인트다.
- **면접 연결:** P2P 세션 관리, 상태 머신 설계, 비동기 스트림(`AsyncStream`으로 거리 이벤트 처리), 폴백 설계.

### F3. 여가 신호 (P1)

**장면:** 점심 먹은 러너가 "15시에 보드게임, 3명" 신호를 켠다. 관심사가 겹치는 러너들에게 푸시가 가고, 피드에 신호가 뜬다. 두 명이 조인하면 참여자 전원의 잠금화면 Live Activity에 "보드게임 15:00 · 3/4명"이 실시간 갱신된다. 정원이 차거나 시간이 지나면 신호는 자동 소멸한다.

- **iOS 기술:** **ActivityKit(Live Activity + Dynamic Island)**, APNs 푸시(신호 상태를 서버 푸시로 갱신), `WidgetKit` 잠금화면 위젯. 신호 수명 관리는 서버 TTL + 클라이언트 로컬 타이머 이중화.
- **왜 이 기술:** "작위적으로 모임을 시작하지 않는다"는 인사이트의 직접 구현 — 모임 개설이 아니라 휘발성 신호다. Live Activity는 앱을 안 열어도 신호가 살아있게 만들고, 세션 시간 내내 잠금화면에 존재감을 만든다. 러너 사용률을 올리는 핵심 장치.
- **면접 연결:** 푸시 파이프라인(APNs token-based auth), Live Activity 갱신 전략(푸시 vs 로컬), 상태 동기화 설계.

### F4. 포항 여가 스팟 맵 (P2)

**장면:** 신호를 만들 때 장소를 고르면 포항 여가 스팟 맵이 열린다. 지자체 데이터 기반 스팟(영일대, 환호공원, 보드게임카페, 스페이스워크 등)이 카테고리별 커스텀 마커로 떠 있고, 스팟마다 "러너들이 여기서 만난 횟수"가 쌓여 있다.

- **iOS 기술:** SwiftUI **MapKit**(`MapContentBuilder`, 커스텀 `Annotation`), Look Around 프리뷰, 스팟 데이터는 지자체 공공데이터 + 수동 시드.
- **왜 이 기술:** "지자체 정보는 양질의 데이터"라는 리서치 결과를 그대로 제품화. 포항이라는 낯선 도시 자체가 온보딩 장벽인데, 맵이 그 장벽을 콘텐츠로 바꾼다.
- **면접 연결:** 지도 성능(클러스터링), 공공데이터 파싱·정규화.

### F5. 겹 기록 — 친밀감 그래프 (P2)

**장면:** 마이페이지에서 "이번 달 7번 만남, 가장 많이 겹친 사람 ○○, 처음 만난 사람 3명"이 차트로 보인다. 카드 맞대기(F2)와 신호 참여(F3)가 자동으로 기록을 만든다 — 수기 입력 없음.

- **iOS 기술:** **SwiftData** 로컬 저장 + **Swift Charts** 시각화 + 홈 화면 위젯(이번 주 겹 횟수). 서버 동기화는 백그라운드 태스크.
- **왜 이 기술:** 친밀감은 주장이 아니라 누적이다. 기록이 쌓여야 "친밀감을 높인다"는 CR이 측정 가능해진다. 이 지표가 그대로 서비스 검증 지표(8장)가 된다.
- **면접 연결:** SwiftData 모델링, 로컬-서버 동기화 전략, 위젯 타임라인.

### F6. 부가 확장 (P3 — v1 이후, 하고 싶은 것 골라서)

- **App Intents / 단축어:** "시리야, 보드게임 신호 켜줘" / 잠금화면 버튼으로 신호 토글.
- **SharePlay:** 원격 러너와 함께 보드게임 룰 영상 보기.
- **NFC 태그:** 아카데미 라운지 테이블에 태그 부착 → 태그하면 그 자리 신호 자동 생성.
- **기수 아카이브:** 기수 종료 시 "우리 기수의 겹" 리캡 (Wrapped 스타일).

## 5. 기술 스택과 아키텍처 — 결정과 근거

면접에서 "왜 썼는지"를 설명 못 하는 스택은 짐이다(디프만 서류 6번 문항, 기술면접 단골). 전부 근거를 갖고 고른다.

| 결정 | 선택 | 근거 (면접에서 말할 그대로) |
|---|---|---|
| UI | **SwiftUI 100%** | 카드·애니메이션·위젯·Live Activity가 제품의 핵심인데 전부 SwiftUI 네이티브 영역. UIKit은 필요한 지점(예: 카메라)에서 `UIViewRepresentable`로만. |
| 동시성 | **Swift Concurrency (async/await, actor)** | GCD 대비 구조화된 동시성 — UWB 거리 스트림, 푸시, 네트워크가 동시에 흐르는 앱이라 취소·수명 관리가 곧 품질. GCD와의 차이를 설명할 수 있어야 하고, 실제로 설명할 것. |
| 상태 관리 | **MVVM + @Observable** (TCA는 채택 안 함) | 혼자 만들고 러너에게 빨리 배포하는 게 목표. TCA는 러닝커브·보일러플레이트 비용이 지금 단계의 이득보다 크다. 다만 단방향 데이터 흐름 원칙은 지킨다 — 이 판단 자체가 면접 답변이 된다. |
| 모듈화 | **SPM 멀티모듈** (Core / DesignSystem / Features) | 빌드 시간과 경계 강제. Tuist는 모듈이 10개를 넘어 프로젝트 파일 관리가 고통이 될 때 도입 — 도구는 문제가 생긴 뒤에 넣는다. |
| 네트워크 | **URLSession 직접 래핑 (POP 기반)** | Alamofire·Moya를 쓰지 않는 이유를 말할 수 있는 것이 오히려 강점. `protocol Endpoint` + 제네릭 디코딩 + `URLProtocol` 목킹으로 테스트 가능하게. OOP/POP 질문의 실전 답안. |
| 백엔드 | **Firebase (Auth·Firestore·FCM) → 인터페이스 뒤에 격리** | 혼자서 실시간 신호·푸시·인증을 2주 안에 굴리려면 BaaS가 맞다. CloudKit은 실시간 프레즌스·복잡 쿼리에서 밀리고, Vapor 자체 서버는 운영 부담이 검증 속도를 죽인다. 단, `SignalRepository` 프로토콜 뒤에 숨겨 도메인 코드가 Firebase를 모르게 한다 — 나중에 자체 서버로 갈아탈 수 있고, 이 격리 설계가 포트폴리오 소재다. |
| 테스트 | **Swift Testing — 도메인·리포지토리 우선** | 커버리지 숫자가 아니라 "무엇을 테스트할 가치가 있다고 판단했는가"를 말한다: 신호 수명 상태 머신, 카드 교환 프로토콜, 네트워크 디코딩. |
| 배포 | **TestFlight + GitHub Actions (Xcode Cloud 대안 비교)** | 러너 실사용이 목표라 배포 자동화는 사치가 아니라 필수. 주 1회 이상 빌드 배포. |

## 6. 마일스톤

팀 노트 기준: "milestone은 월요일 저녁~화요일까지 내가 개발한 게 나오면 best" → 48시간 데모를 첫 관문으로 잡는다.

### M0 — 48시간 데모 (월요일 저녁 ~ 화요일)
보여줄 것 하나만: **정체성 카드 생성 → 두 대의 아이폰으로 카드 맞대기 교환.**
서버 없음. 전부 온디바이스 + P2P. 재성(멘토) 피드백은 이 데모 위에서 받는다.
- 카드 생성 화면 (MeshGradient 비주얼, 관심사 3개 선택)
- MultipeerConnectivity 교환 (UWB는 M1로 미룸 — 48시간 안에는 MPC까지가 현실적)
- 받은 카드 목록 + 겹치는 관심사 하이라이트

### M1 — 2주 MVP (TestFlight, 러너 20명)
- F1 완성 + F2에 UWB 거리 연출 추가
- F3 여가 신호: 생성·조인·푸시·자동 소멸 (Live Activity 포함)
- Firebase 연동, 러너 인증(아카데미 이메일 도메인 화이트리스트)
- TestFlight 배포 → 같은 세션 러너 20명 온보딩

### M2 — 6주 v1 (러너 전체 개방)
- F4 포항 스팟 맵, F5 겹 기록·위젯
- 신호 응답률·재사용률 데이터 기반 개선 1사이클
- 다음 기수 인수인계 문서 (서비스가 기수를 넘어 살아남는 조건)

### M3 — 지원 시즌 전 (동아리 모집 일정 역산)
- 회고 블로그 3편 이상 (아래 8장 주제)
- 앱스토어 심사 도전 (아카데미 외부인 가입 제한 문제 해결 필요 — 심사 통과 자체가 스토리)

## 7. 실사용 검증 — "실제 러너들이 쓴다"의 정의

목표를 숫자로 박는다. 세션 러너 모수를 100~150명으로 가정.

- **설치:** 세션 러너의 50% 이상
- **핵심 지표 — 주간 겹 발생 수:** 카드 교환 + 신호 성사 합계가 주 30건 이상
- **신호 응답률:** 켜진 신호 중 1명 이상 조인이 60% 이상 (이 숫자가 "작위적이지 않은 만남" 가설의 검증치)
- **4주 리텐션:** 설치자의 30%가 4주 차에도 주 1회 이상 사용

숫자가 안 나오면 기능을 늘리는 게 아니라 루프의 어느 고리가 끊겼는지(카드가 안 예쁜가, 신호 문턱이 여전히 높은가)를 찾는다. 이 과정 자체가 면접의 "실패와 원인 분석" 문항 답이 된다.

## 8. 동아리 지원과의 연결 — 기능이 곧 답변이 되게

세 후기(디프만 15기 서류·면접, 디프만 iOS 14기, SeSAC 3기)에서 뽑은 공통 요구사항에 이 프로젝트를 정렬한다.

| 면접 단골 질문 | 이 프로젝트에서 나오는 실전 답 |
|---|---|
| Class vs Struct | 정체성 카드를 값 타입으로 설계한 이유 — 교환은 스냅샷 복사, 원본 불변 |
| GCD / 동시성 | GCD 대신 Swift Concurrency를 택한 근거, UWB 거리 이벤트를 `AsyncStream`으로 다룬 실경험 |
| OOP와 POP | `Endpoint` 프로토콜 기반 네트워크 레이어, 리포지토리 추상화로 Firebase 격리 |
| RxSwift/Combine 왜 쓰나 | "안 썼다"를 근거 있게: Observation + AsyncSequence로 충분했던 이유와 한계 인지 |
| 기술 스택 각각의 이유 (서류 1000자) | 5장 표를 그대로 옮기면 된다 — 전부 트레이드오프 언어로 적혀 있음 |
| 배포 경험 | TestFlight 운영 + 실사용자 지표로 개선한 1사이클, "배포는 끝이 아니라 시작" 실화 |
| 협업/갈등 | 팀 CBL 과정(압박면접으로 GQ 선정, 리서치 분담)과 멘토 피드백 반영 이야기 |

**서류 스토리 한 줄 버전:** "같은 건물에 100명의 iOS 개발자 지망생이 있는데 서로 친해질 도구가 없어서, UWB로 아이폰을 맞대면 정체성 카드가 교환되는 앱을 만들어 러너 절반이 쓰게 했다." — 문제 발견, 기술 선택, 실사용 검증이 한 문장에 다 들어간다.

**회고 블로그 주제 (M3 전까지 3편):**
1. NearbyInteraction + MultipeerConnectivity로 NameDrop 같은 카드 교환 만들기 (폴백 설계 포함)
2. Live Activity를 푸시로 갱신하기 — 여가 신호의 수명 관리
3. Firebase를 리포지토리 뒤에 숨긴 이유 — BaaS와 이별할 수 있는 아키텍처

## 9. 리스크와 정면 대응

- **UWB 권한·기기 파편화:** NearbyInteraction은 iPhone 11+ U1 칩 필요. → MPC 폴백을 처음부터 1급 시민으로 설계. 데모는 폴백 경로까지 보여준다.
- **양면 시장 콜드스타트:** 신호를 켰는데 아무도 없으면 죽는다. → M1을 "같은 세션 20명" 밀집 집단으로 시작, 초기 2주는 내가 매일 신호를 켠다(창업자가 첫 유저).
- **아카데미 정책(외부 서버·개인정보):** 러너 데이터 취급 범위를 아카데미 운영진과 사전 합의. 최소 수집 원칙(이메일·닉네임·관심사만).
- **혼자 개발 번아웃:** P1 세 기능 외에는 전부 뒤로. 이 문서의 P 라벨이 곧 방어선이다.
- **팀 프로젝트와의 경계:** 팀 CBL 산출물(컨셉·리서치)은 팀의 것 — 이 레포는 그 위에 얹는 개인 기술 확장임을 팀과 재성에게 투명하게 공유하고 피드백 받는다 (팀 노트에 이미 "기획 가져가면 재성이 피드백 준다"고 합의됨).

---

## 다음 액션 (이번 주)

1. **오늘:** 이 문서를 재성 피드백용으로 요약 1페이지로 추림 → 내일 피드백
2. **월~화 (M0):** 카드 생성 + MPC 교환 데모 — Xcode 프로젝트 생성, DesignSystem/Core 모듈 골격
3. **피드백 반영 후:** M1 백로그를 이슈로 쪼개고 2주 스프린트 시작

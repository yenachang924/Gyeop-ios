# 겹 오퍼레이터 — 운영 에이전트 설계 명세

겹의 지표·로그·제보를 매일 읽고 **제품 문제를 먼저 제안하는 에이전트.** v0.2~v0.3에 붙인다.

두 가지를 동시에 노린다: ① 혼자 운영하는 앱에서 "숫자를 매일 보는 사람"을 대체 ② 당근 로컬잡스가 요구하는 Observability·Memory·Evaluation 구조의 실물 사례.

핵심 설계 원칙: **프롬프트를 잘 쓰는 게 아니라 룰·컨텍스트·도구를 설계한다.** 그리고 **제안의 적중률을 채점해서 룰이 스스로 갱신되게 한다.** 채점 루프가 없으면 그냥 요약봇이다.

---

## 1. 구조

```
[수집]                    [판단]                [행동]              [학습]
Firestore 일별 집계  ─┐
OSLog 원격 수집      ─┼→  룰 엔진 + LLM  →  GitHub Issue 생성  →  2주 뒤 채점
크래시 리포트        ─┤    (컨텍스트:        Discord 요약 포스팅      ↓
GitHub Issues(제보)  ─┤     메모리 + 룰)                        룰 가중치 갱신
앱스토어 리뷰        ─┘                                              ↓
                                                              다음 판단에 반영
```

## 2. Observability — 무엇을 보는가

| 소스 | 수집 항목 | 주기 |
|---|---|---|
| Firestore | 설치 수, DAU 아닌 **겹 발생 수**, 신호 생성/조인/성사, 성사까지 걸린 시간, 팀 밖 겹 비율 | 일 1회 배치 |
| 앱 원격 로그 | 맞대기 실패율·구간별 지연(발견/연결/전송), 동기화 큐 적체, 푸시 수신률 | 실시간 수집, 일 집계 |
| 크래시 | 신규 크래시 시그니처, 영향 사용자 수, 발생 버전 | 발생 즉시 |
| GitHub Issues | 러너 제보 본문·라벨 | 발생 즉시 |
| App Store | 리뷰 별점·본문 | 일 1회 |

수집은 Cloud Functions 스케줄러가 하나의 **일일 스냅샷 JSON**으로 만들어 에이전트에 넘긴다. 에이전트가 여러 API를 직접 호출하지 않게 하는 게 안정성의 핵심이다.

## 3. 룰 — 판단의 뼈대 (프롬프트가 아니라 룰)

룰은 `rules.yaml`로 관리하고, 각 룰에 **가중치**와 **최근 적중률**이 붙는다.

```yaml
- id: signal-success-drop
  when: "신호 성사율이 3일 연속 40% 미만"
  then: "원인 가설 3개와 각각의 검증 방법을 제시. 반드시 포함할 후보:
         (a) 알림 도달 시각이 활동 시간과 어긋남
         (b) 관심사 매칭 범위가 좁아 대상 인원 부족
         (c) 신호 TTL이 너무 짧아 조인 기회 소멸"
  weight: 1.0
  hit_rate: null

- id: exchange-failure-spike
  when: "맞대기 실패율이 전주 대비 2배 이상"
  then: "구간별 지연 분포를 비교해 발견/연결/전송 중 어디가 악화됐는지 지목하고,
         해당 구간에 영향 준 최근 커밋 후보를 나열"
  weight: 1.0

- id: silent-cohort
  when: "설치 후 7일간 겹 0건인 러너가 30% 이상"
  then: "온보딩 이탈 지점을 로그로 추적하고, 첫 겹까지의 마찰 요인을 제안"
  weight: 1.2   # 리텐션 직결이라 가중 상향

- id: crash-new-signature
  when: "신규 크래시 시그니처 발생"
  then: "스택 트레이스와 최근 3커밋 diff를 대조해 원인 후보 지목. 즉시 이슈 생성"
  weight: 2.0
```

**룰이 하는 일:** LLM에게 "알아서 분석해줘"라고 던지지 않는다. 언제 발화할지, 무엇을 반드시 검토할지를 룰이 고정하고, LLM은 그 안에서 데이터를 해석한다. 이게 재현성을 만든다.

## 4. Memory — 축적되는 것

```json
{
  "proposal_id": "2026-09-12-signal-timing",
  "triggered_by": "signal-success-drop",
  "observation": "성사율 34% (3일 평균), 신호의 62%가 14~16시 생성, 조인은 18시 이후 집중",
  "hypothesis": "신호 생성 시각과 러너 활동 시각의 불일치",
  "proposed_action": "신호 기본 시작 시간을 18시로, 알림 발송을 17시 30분으로",
  "adopted": true,
  "adopted_at": "2026-09-13",
  "outcome_measured_at": "2026-09-27",
  "outcome": { "signal_success_rate": "34% → 46%", "verdict": "hit" },
  "note": "다만 주말에는 효과 없음 — 요일별 분리 필요"
}
```

메모리가 다음 판단에 들어가는 방식:
- **같은 룰이 재발화하면** 과거 제안과 결과를 컨텍스트에 넣는다 → "지난번 시간 조정은 평일만 효과 있었으니, 이번엔 요일별로 나눠 제안"
- **채택되지 않은 제안**도 남긴다 → 반복 제안 방지, 또는 "3번 제안했으나 미채택" 자체가 신호
- **주말/시험기간 같은 계절성**을 누적 관측으로 학습

## 5. Evaluation — 스스로 나아지는 루프

제안이 나온 지 **2주 후** 자동 채점한다. 이게 요약봇과 에이전트를 가르는 지점이다.

| 판정 | 조건 | 룰 가중치 변화 |
|---|---|---|
| hit | 채택 + 목표 지표가 유의미하게 개선 | ×1.2 |
| miss | 채택했으나 개선 없음/악화 | ×0.8 |
| unadopted | 미채택 | 변화 없음, 3회 누적 시 룰 재검토 플래그 |

채점 결과는 메모리에 쌓이고, 가중치가 낮아진 룰은 발화 임계값이 올라간다(덜 시끄러워진다). **적중률이 낮은 룰은 조용해지고, 잘 맞히는 룰은 더 자주 말한다.**

추가 평가 축: 제안의 **신선도**(같은 제안 반복 시 감점), **실행 가능성**(구체적 액션이 있는가), **근거의 데이터 인용 여부**.

## 6. 도구 연결 (MCP)

| 도구 | 용도 |
|---|---|
| Firestore MCP (또는 Functions 엔드포인트) | 일일 스냅샷 조회 |
| GitHub MCP | 이슈 생성, 최근 커밋·diff 조회(크래시 원인 대조) |
| Discord/Slack MCP | 러너 채널에 주간 요약 포스팅, 제보 수집 |

**핸드오프 제거가 목적이다.** 사람이 "지표를 열어보고 → 판단하고 → 이슈를 쓰는" 3단계를, 에이전트가 스냅샷→판단→이슈까지 한 번에 통과시킨다. 사람은 이슈를 읽고 채택 여부만 결정한다.

## 7. 구현 단계

| 단계 | 범위 | 시점 |
|---|---|---|
| Lv.1 | 일일 스냅샷 수집 + 룰 4개 + 요약 리포트 생성 (수동 실행) | v0.2 |
| Lv.2 | 스케줄 자동 실행 + GitHub 이슈 자동 생성 + 메모리 저장 | v0.2 |
| Lv.3 | 2주 후 자동 채점 + 룰 가중치 갱신 | v0.3 |
| Lv.4 | 러너 채널 주간 포스팅, 제보를 룰 트리거로 연결 | v0.3+ |

## 8. 실패 모드와 대비 (아티클 소재이자 면접 소재)

| 실패 모드 | 대비 |
|---|---|
| 데이터가 적어(150명) 통계적으로 무의미한 변동에 반응 | 최소 표본 조건을 룰에 명시(예: 신호 10건 이상일 때만 성사율 판단) |
| 그럴듯하지만 틀린 인과 추론 | 제안에 반드시 **검증 방법**을 포함하게 강제. 검증 없는 제안은 반려 |
| 매일 시끄러워서 무시하게 됨 | 가중치 하향 + 발화 임계값 + "오늘은 보고할 것 없음"을 정상 출력으로 허용 |
| 채점의 인과 오귀속 (다른 변화 때문에 개선) | 채점 시 동시 변경 사항을 함께 기록, 단정 대신 신뢰도 표기 |

## 9. 이게 왜 의미 있나

혼자 운영하는 앱의 가장 흔한 실패는 **기능은 계속 만드는데 숫자를 안 보는 것**이다. 파운데이션이 끝나고 혼자 남았을 때, 매일 아침 "어제 겹 4건, 성사율 38%, 3일 연속 하락 — 가설 3개" 를 읽어주는 존재가 있는 것과 없는 것은 다르다.

에이전트를 만드는 게 목적이 아니라, **혼자서도 데이터를 보고 판단하는 운영 체계를 갖는 것**이 목적이다.

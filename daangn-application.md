# 당근 로컬 잡스 SWE 인턴 — 지원 작업 문서

**마감: 8월 9일(일) 23:59** (조기 마감 가능 · 마감 직전 트래픽 몰림 → 최소 3시간 여유 두고 제출)
전형: 서류(사전질문 4) → 직무 인터뷰 → 최종. 3개월 체험형, 합격 시 8/24 입사.

## 0. 포지션 오독 방지

이 자리는 **AI 에이전트 엔지니어**다. iOS도 백엔드도 아니다. 겹의 UWB·Live Activity는 가산점이 아니다.

공고가 반복하는 키워드: **룰 · 컨텍스트 · 도구**(프롬프트가 아니라), **워크플로우 설계**, **핸드오프 제거**, **Evaluation · Observability · Memory**, **지속적으로 발전하는 시스템**.

그러므로 답변의 주인공은 겹이라는 앱이 아니라 **"겹을 5일 만에 만들기 위해 내가 설계한 에이전트 워크플로우"**다. 겹은 그 워크플로우가 실제 결과를 낸 증거로 등장한다.

## 1. 관통 메시지 (4문항 전체에 흐를 한 줄)

> **"저는 에이전트에게 일을 시키는 게 아니라, 에이전트가 일할 환경(룰·컨텍스트·도구)을 설계하고, 그 결과를 채점해서 환경을 고칩니다."**

각 답변은 이 문장의 서로 다른 증거가 되어야 한다.

---

## 2. Q1. 최근 가장 흥미롭게 실험한 AI 기술

**핵심 프레임:** 기술 이름 자랑이 아니라 **"프롬프트로 안 되던 것이 룰·컨텍스트 설계로 됐다"**는 전환점 하나.

**구성:**
1. 처음엔 프롬프트로 해결하려 했다 — 그때의 구체적 실패 (재현되지 않음, 매번 다른 결과, 맥락 상실)
2. 무엇을 바꿨나 — 프로젝트 룰 파일, 문서 컨텍스트 계층, 도구 접근 범위 설계
3. 무엇이 달라졌나 — **측정 가능한 차이** (예: 같은 작업 재요청 시 결과 일관성, 되돌린 커밋 수, 리뷰 왕복 횟수)
4. 아직 안 되는 것 — 한계를 아는 사람이라는 증거

> ✍️ **채워야 할 것 (D2~D4 개발 중 기록):** 겹 개발에서 룰 파일에 무엇을 적었을 때 무엇이 달라졌는지 구체 사례 2개. 특히 "에이전트가 자꾸 틀리던 것 → 룰로 고정 → 안 틀림" 케이스.

## 3. Q2. 최근 6개월 동안 AI를 활용해 가장 크게 변화를 만든 경험

**핵심 프레임:** 속도 자랑이 아니라 **위임과 판단의 분리**. "무엇을 맡기고 무엇을 직접 판단했는가"가 답의 척추.

**사례:** 겹 — 개인 프로젝트를 5일 개발 → App Store 심사 제출 → 실사용자 확보까지.

**구성:**
1. 상황: 혼자, 5일, 서버+iOS+심사까지. 전통적 방식으로는 불가능한 스코프
2. 워크플로우 설계: 문서를 컨텍스트로 계층화(제품 명세 / 구현 범위 / 기술 결정) → 에이전트가 매번 같은 전제 위에서 일하게 함
3. **직접 판단한 것:** 컷라인(무엇을 버릴지), 기술 선택의 트레이드오프, 심사 리스크. 위임하지 않은 이유를 명시
4. 결과: 수치로 (출시 여부, 사용자 수, 겹 발생 수)
5. 배운 것: 에이전트의 성능은 모델이 아니라 **내가 준 컨텍스트의 품질**에 비례했다

> ✍️ **채워야 할 것 (D5 이후):** 실제 결과 수치. 미제출/미출시 상태라면 **"현재 진행형"으로 정직하게** 쓰고 심사 제출 상태를 명시한다. 없는 결과를 있다고 쓰면 인터뷰에서 무너진다.

## 4. Q3. 데이터를 기반으로 제품의 문제를 제안하는 에이전트

**→ 겹 오퍼레이터(`gyeop-operator-spec.md`)가 그대로 답이다. 설계도가 아니라 만들고 있는 것이라는 게 차별점.**

**답변 골격 (초안):**

제가 운영하는 앱에 붙이고 있는 구조로 답하겠습니다. 요약봇과 에이전트를 가르는 건 **채점 루프**라고 봅니다.

**① 수집(Observability) — 에이전트가 API를 직접 호출하지 않게 한다**
지표·로그·크래시·사용자 제보를 스케줄러가 **하나의 일일 스냅샷**으로 만들어 넘깁니다. 에이전트가 여러 소스를 직접 쿼리하면 실패 지점이 늘고 재현이 안 됩니다. 수집과 판단을 분리하는 게 안정성의 출발입니다.

**② 판단(룰) — "알아서 분석해줘"를 금지한다**
언제 발화할지와 무엇을 반드시 검토할지를 룰로 고정하고, LLM은 그 안에서 데이터를 해석하게 합니다.
예: `신호 성사율이 3일 연속 40% 미만이면 → 원인 가설 3개와 각각의 검증 방법 제시. 알림 도달 시각/매칭 범위/TTL을 반드시 후보에 포함`
룰마다 **최소 표본 조건**을 둡니다. 사용자가 적은 초기 제품에서 통계적으로 무의미한 변동에 반응하는 게 가장 흔한 실패라서요.

**③ 기억(Memory) — 제안·채택 여부·결과를 한 레코드로**
같은 룰이 재발화하면 과거 제안과 그 결과를 컨텍스트에 넣습니다. "지난번 알림 시간 조정은 평일만 효과가 있었으니 이번엔 요일별로 나눠 제안" 같은 판단이 여기서 나옵니다. 채택되지 않은 제안도 남깁니다 — 반복 제안을 막고, "3번 제안했으나 미채택" 자체가 신호가 됩니다.

**④ 평가(Evaluation) — 2주 뒤 자동 채점, 결과가 룰을 바꾼다**
제안이 채택됐고 목표 지표가 개선됐으면 hit, 채택했는데 변화 없으면 miss. 적중률이 낮은 룰은 발화 임계값이 올라가 **조용해지고**, 잘 맞히는 룰은 더 자주 말합니다. 인과 오귀속을 막기 위해 채점 시 동시 변경 사항을 함께 기록하고, 단정 대신 신뢰도를 표기합니다.

**⑤ 행동 — 핸드오프 제거**
사람이 "지표를 열어보고 → 판단하고 → 이슈를 쓰는" 3단계를 스냅샷→판단→이슈 생성으로 통과시킵니다. 사람은 채택 여부만 결정합니다.

**제가 경계하는 실패:** 그럴듯하지만 틀린 인과 추론. 그래서 모든 제안에 **검증 방법을 반드시 포함**하도록 강제하고, 검증 방법이 없는 제안은 출력에서 반려합니다. 그리고 "오늘은 보고할 것 없음"을 정상 출력으로 허용합니다 — 매일 시끄러운 에이전트는 결국 무시당하니까요.

## 5. Q4. 회의 일정을 예약하는 사내 에이전트

**→ 겹의 여가 신호와 같은 문제 구조다. 이 연결을 첫 문장에 놓는다.**

**답변 골격 (초안):**

이 문제는 제가 만들고 있는 앱의 핵심 메커니즘과 같은 구조라 흥미롭습니다. 여러 사람의 가용 시간을 모아 만남을 성사시키되, **거절의 사회적 비용을 없애는 것**이 본질이라고 봅니다. 캘린더 API를 붙이는 건 이 문제의 절반도 안 됩니다.

**① 진짜 문제는 빈 시간 찾기가 아니다**
캘린더의 빈칸은 "가능한 시간"이 아닙니다. 사람마다 회의를 넣고 싶지 않은 시간대, 집중 시간, 이동 시간이 있고 그건 캘린더에 없습니다. 그래서 에이전트는 **관측으로 이를 학습해야** 합니다 — 이 사람이 실제로 수락한 시간대, 거절하거나 옮긴 시간대, 회의 직후 취소 패턴을 메모리에 쌓습니다.

**② 룰이 먼저, 최적화는 그다음**
`이동 필요한 오프라인 회의는 앞뒤 15분 버퍼`, `4명 이상이면 오전 우선(오후는 조정 실패율이 높음)`, `주니어의 집중 시간은 시니어 편의보다 우선` 같은 조직의 규범을 룰로 명시합니다. 이런 규범은 LLM이 추론할 수 없고, 조직마다 다르며, **명시적으로 관리·수정되어야** 합니다.

**③ 제안은 상호적이어야 한다 — 거절 비용 제거**
특정인에게 "이 시간 되세요?"를 직접 물으면 거절 부담이 생기고 응답률이 떨어집니다. 대신 후보 슬롯 2~3개를 동시에 제시하고, 참석자가 각자 가능 여부만 표시하면 에이전트가 확정합니다. 응답하지 않은 사람은 "거절"이 아니라 "제약 없음"으로 처리해 침묵이 병목이 되지 않게 합니다. (제 앱에서는 이 구조로 모임 제안의 심리적 문턱을 없앴습니다.)

**④ 실패 경로를 먼저 설계**
성사 실패 시 무엇을 하는가가 실사용 품질을 가릅니다. 조율 3회 실패 → 비동기 대체 제안(문서 코멘트로 대체 가능한 안건인지 판단) → 그래도 필요하면 최소 인원으로 축소 제안. **"회의를 안 여는 것"도 정상 결과**로 둡니다.

**⑤ 평가 지표**
예약 성공률이 아니라 ① 조율 왕복 횟수(사람의 개입 횟수), ② 예약 후 취소·변경률, ③ 참석자의 사후 만족. 특히 ②가 중요합니다 — 억지로 잡은 회의는 예약엔 성공하고 실제론 실패한 것이니까요. 이 지표들이 다시 룰의 가중치를 조정합니다.

---

## 6. 개발 중 기록 템플릿 (D2~D5, 매일 5분)

지원서는 따로 쓸 시간이 없다. **개발하며 아래만 채워두면 D5~D6에 문장으로 옮기기만 하면 된다.**

```
[날짜]
- 에이전트에게 위임한 것:
- 내가 직접 판단한 것 (그리고 왜 위임하지 않았나):
- 룰/컨텍스트를 고친 순간 (무엇이 틀렸고 → 무엇을 명시했더니 → 어떻게 달라졌나):
- 측정된 차이 (있다면 수치):
- 안 되던 것 / 한계:
```

## 7. 제출 전 체크

- [ ] 4문항 모두 **"룰·컨텍스트·도구"** 언어로 쓰였는가 (프롬프트 이야기만 하고 있지 않은가)
- [ ] Q1·Q2에 **측정된 차이**가 들어갔는가
- [ ] Q3·Q4에 **실패 모드와 평가 지표**가 들어갔는가 (이게 시니어리티 신호)
- [ ] 겹의 진행 상태를 **정직하게** 썼는가 (미출시면 미출시라고)
- [ ] 8/9 20:00 이전 제출 (마감 트래픽 회피)

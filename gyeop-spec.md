# 겹(Gyeop) — 개인 프로젝트 기능 명세 (제약 해제판)

> 이 문서는 팀 CBL 프로젝트와 별개의 **개인 프로젝트** 명세다. 팀 산출물(컨셉 리서치)은 출발점이었을 뿐, 여기서부터는 문제 정의도 기능도 새로 세운다. 기술 제약 없음: 서버, 와이파이, 푸시, 계정 전부 사용한다.

---

## 1. 문제 정의 — 세게

**아카데미는 9개월짜리 유한한 공동체다. 150명이 같은 건물에 있지만, 대부분은 서로 "아는 사람"으로 끝난다.**

수료식 날 돌아보면 진짜 친해진 사람은 10명 남짓이다. 나머지 140명은 얼굴과 이름만 아는 채로 흩어진다. 같은 시기에 같은 도시에서 같은 꿈을 꾼, 평생 갈 수도 있었던 동료들인데.

왜 이렇게 되는가. 손실의 구조는 세 개다.

### 손실 1 — 친밀감이 우연에 맡겨져 있다
누구와 친해지는가는 팀 배정, 자리 운, 흡연 여부, MBTI가 아니라 **동선의 우연**이 결정한다. 관계 그래프는 세션 팀 클러스터 안에 갇히고, 클러스터 밖 145명은 접점 자체가 없다. 우연은 공평하지 않다 — 내향적인 사람일수록, 늦게 합류한 사람일수록 우연의 몫이 작다.

### 손실 2 — 먼저 제안하는 사람이 비용을 다 낸다
"오늘 저녁에 보드게임 하실 분?"이라고 먼저 말하는 사람이 거절의 위험, 어색함, "쟤 왜 저래" 리스크를 혼자 부담한다. 그래서 모두가 제안을 미루고, 각자 기숙사로 흩어진다. **놀고 싶은 사람은 많은데 제안하는 사람이 없는 상태** — 이게 아카데미 여가시간의 기본값이다.

### 손실 3 — 9개월이 길다고 착각한다
초반엔 "언젠가 친해지겠지"라며 미루고, 후반엔 프로젝트에 치여 미루고, 수료식에서 후회한다. 시간의 유한함이 일상에서 보이지 않기 때문이다. 포항이라는 도시도 마찬가지다 — "나중에 가봐야지" 하던 영일대 일몰을 못 보고 떠난다.

### 한 줄 정의

**겹은 우연을 설계로 바꾼다.** 친해짐을 운에 맡기지 않고, 제안 비용을 시스템이 대신 내고, 유한한 시간을 눈에 보이게 만든다.

## 2. 해결 원칙 (기능 설계의 헌법)

1. **만남의 원자는 오프라인이다.** 앱 안에서 친해지는 게 아니라, 앱은 만남의 발화·인증·기록만 맡는다. 카드 맞대기(물리적 근접)가 모든 기록의 유일한 진실 소스다 — 앱상 채팅 천 마디보다 맞댐 한 번.
2. **제안은 익명적이거나 상호적이어야 한다.** 거절이 성립하지 않는 구조(휘발 신호, 상호 매칭)만 쓴다. 특정인에게 1:1로 "놀자"를 보내는 기능은 만들지 않는다.
3. **유한함을 상시 노출한다.** 수료 D-day가 앱의 시계다. 모든 기록은 "끝이 있는 시즌"의 기록이다.
4. **팀 밖으로 밀어낸다.** 지표·보상·매칭이 전부 "안 만나본 사람"에 가중치를 둔다. 이미 친한 사람끼리 더 친해지는 건 앱이 도울 일이 아니다.

## 3. 핵심 개념·데이터 모델

```
Runner    — 러너 계정 (아카데미 이메일 인증, 세션/팀 메타데이터)
Card      — 정체성 카드 (관심사 시드, 비주얼 시드, 버전 이력)
Gyeop     — "겹" = 인증된 만남 1회 (카드 맞대기 or 신호 성사 + 위치)
Signal    — 휘발성 여가 신호 (활동, 시각, 정원, 스팟, TTL)
Match     — 주간 룰렛 매칭 (두 러너, 마감 시한, 성사 여부)
Spot      — 포항 여가 스팟 (지자체 데이터 + 러너 누적 기록)
Season    — 기수 (시작일, 수료일, 전체 통계)
```

관계의 핵심: `Gyeop`은 반드시 물리적 인증(UWB 맞대기 또는 신호+위치)에서만 생성된다. 서버에서 임의 생성 불가 — 이 무결성이 서비스의 신뢰다.

## 4. 구현 기능

### F1. 정체성 카드 — "나를 들고 다닌다"

**동작:** 온보딩에서 관심사(최대 5)·여가 성향(정적/동적 × 실내/실외)·한 줄을 입력하면 시드 기반 제너러티브 비주얼 카드가 생성된다. 같은 입력 = 같은 카드(결정적), 다른 입력 = 세상에 하나뿐인 카드. 관심사가 바뀌면 카드도 "자라며" 버전 이력이 남는다.

- **기술:** SwiftUI `MeshGradient` + Metal `layerEffect` 셰이더 (시드 → 색·패턴·질감 결정 함수), `matchedGeometryEffect` 전환, 카드는 값 타입 스냅샷
- **수용 기준:** 온보딩 3분 내 완료. 카드 렌더 60fps. 동일 시드 → 픽셀 동일 재현.

### F2. 카드 맞대기 — 만남의 인증 원자

**동작:** 두 러너가 아이폰 윗부분을 맞대면 NameDrop풍 물결 애니메이션과 함께 카드가 교환되고, **겹 1회가 기록된다.** 겹치는 관심사가 그 자리에서 하이라이트되어 첫 대화 소재를 만든다("어, 너도 클라이밍?").

- **기술:** MultipeerConnectivity(발견·전송) + NearbyInteraction(UWB 거리·방향, 30cm 이내에서 트리거) + CoreHaptics. U1 미지원/권한 거부 시 MPC 근접 발견 + 양측 확인 버튼 폴백. 교환 데이터는 서버 동기화(오프라인 큐잉 → 재연결 시 업로드).
- **수용 기준:** 맞댐 → 교환 완료 3초 이내. 네트워크 없이도 교환 성립(로컬 저장 후 동기화). 동일 상대 재맞댐은 24시간 1회만 겹으로 집계(어뷰징 방지).

### F3. 여가 신호 — 거절이 성립하지 않는 제안

**동작:** "19시 보드게임, 라운지, 4명"을 켜면 관심사 겹치는 러너에게 푸시가 가고 피드에 뜬다. 조인은 익명 카운트로만 보이다가 정원이 차거나 본인이 공개하면 드러난다 — **제안자도 조인자도 거절당할 수 없다.** 성사되면 참여자 전원의 잠금화면 Live Activity가 "보드게임 19:00 · 3/4"로 실시간 갱신되고, 시간이 지나면 신호는 증발한다. 신호에는 최소한의 조율 스레드(장소 바꿀까요? 수준)만 붙는다 — 풀 채팅은 만들지 않는다. 잡담은 카톡이 이기는 싸움이고, 우리 앱의 일이 아니다.

- **기술:** Firestore 실시간 리스너(피드), FCM→APNs 푸시, ActivityKit Live Activity **푸시 갱신**(p8 token auth, stale date로 자동 소멸), 서버 TTL(Cloud Functions 스케줄러)
- **수용 기준:** 신호 생성 30초 이내(마찰 최소). 푸시 도달 5초 이내. 신호는 TTL 후 서버·클라이언트 모두에서 완전 소멸(휘발성 = 심리적 안전).

### F4. 겹 그래프 — 팀 밖으로 밀어내는 지도

**동작:** 세션 전체의 관계 지도를 본다. 나를 중심으로 겹이 있는 사람은 또렷하게, 없는 사람은 실루엣으로. 핵심 지표는 하나 — **"팀 밖 겹"**. "당신의 겹 23명 중 팀 밖은 4명입니다. 아직 안 만난 러너 127명." 매주 팀 밖 겹이 늘면 카드에 새 질감이 추가된다(보상도 팀 밖 가중).

- **기술:** SwiftUI `Canvas` force-directed 그래프(시뮬레이션은 오프메인 actor에서, 렌더는 TimelineView), 서버 집계는 익명화(타인 간 겹은 노드 연결만, 상세 비공개)
- **수용 기준:** 150노드 60fps. 타인의 관계 상세는 절대 노출 안 됨(내 겹만 상세).

### F5. 주간 겹 룰렛 — 제안 비용을 시스템이 낸다

**동작:** 매주 월요일 아침, **아직 겹이 없는 러너** 중 관심사가 겹치는 1명과 상호 매칭된다. 둘 다 수락하면 미션이 뜬다: "이번 주 안에 카드를 맞대세요. 추천 스팟: 영일대 카페 ○○." 성사되면 둘 다 보너스 겹. 미성사여도 페널티 없음, 다음 주 새 룰렛. 매칭은 시스템이 했으므로 누구도 먼저 제안한 사람이 아니다 — 손실 2의 정면 해결책.

- **기술:** Cloud Functions 크론 매칭(관심사 자카드 유사도 + 미겹 필터 + 팀 밖 가중), 수락/성사 상태 머신, 월요일 아침 푸시
- **수용 기준:** 매칭 수락률 50%, 수락 후 성사율 40%를 초기 목표 지표로. 3주 연속 미수락 시 룰렛 자동 중단(스팸화 방지) 후 옵트인 복귀.

### F6. 포항 스탬프 — 도시를 함께 소진한다

**동작:** 지자체 데이터 기반 포항 스팟 맵(영일대, 환호공원, 스페이스워크, 구룡포, 보드게임카페…). 스팟에서 함께 있는 상태로 카드를 맞대면 **그 스팟의 스탬프**가 둘 모두에게 찍힌다. 스팟마다 "이번 기수 러너 방문 37회" 누적이 보이고, 아무도 안 가본 스팟은 "미개척"으로 표시되어 첫 방문 팀에게 첫 발자국 배지를 준다.

- **기술:** MapKit(SwiftUI, 커스텀 어노테이션·클러스터링), CoreLocation 지오펜스 검증(스팟 반경 100m 내 맞대기만 스탬프 인정), 공공데이터 파싱 + 수동 시드 30스팟
- **수용 기준:** 위치 검증 오탐 방지(반경 밖 맞대기는 일반 겹으로만). 스팟 시드 데이터 30개로 시작.

### F7. 시즌 시계 — 유한함을 상시 노출

**동작:** 앱 어디에나 수료 D-day가 흐른다. 홈 위젯 "D-183 · 이번 주 겹 2회". 매월 말 월간 리캡("10월: 새 겹 6명, 첫 구룡포"). 수료 주간에는 기수 전체 Wrapped — "우리 기수는 9개월간 3,214번 겹쳤다. 최다 겹 스팟: 영일대." 개인 Wrapped는 카드 이력과 함께 아카이브로 영구 보존.

- **기술:** WidgetKit(홈·잠금화면 액세서리, App Group 공유), 서버 집계 배치, Wrapped는 SwiftUI 애니메이션 시퀀스 + 이미지 렌더(`ImageRenderer`)로 공유 가능
- **수용 기준:** 위젯 타임라인 예산 내 갱신 설계. Wrapped 공유 이미지 생성 3초 이내.

### F8. App Intents — 시스템에 심기

**동작:** "시리야, 겹 신호 켜줘" / 잠금화면 버튼·액션버튼으로 신호 토글 / 단축어 "퇴근 신호"(매일 18시 제안). 앱을 열지 않아도 겹이 작동한다.

- **기술:** `AppIntent` + `AppShortcutsProvider`, ControlCenter/잠금화면 컨트롤(iOS 18 ControlWidget)
- **수용 기준:** 신호 켜기까지 앱 실행 0회.

## 5. 명시적 비기능 (안 만드는 것)

- **풀 채팅** — 카톡을 이길 수 없고 이길 필요도 없다. 조율 스레드까지만.
- **1:1 지목 제안** — 해결 원칙 2 위반. 거절이 성립하는 순간 앱이 부담이 된다.
- **좋아요/팔로우/피드 소셜** — 온라인 체류시간은 우리의 적이다. 앱 체류시간이 아니라 오프라인 겹 수가 지표다.
- **친밀도 점수 랭킹 공개** — 관계의 서열화는 커뮤니티를 부순다. 내 지표는 나만, 전체 지표는 익명 집계만.

## 6. 기술 스택 (제약 해제 반영)

기존 `runner-app-plan.md` 5장의 결정을 유지하되, 서버 사용이 전면 허용되므로:

- **백엔드:** Firebase(Auth·Firestore·FCM·Cloud Functions·Storage). 룰렛 매칭·TTL·집계는 Cloud Functions. 리포지토리 프로토콜 격리는 유지 — 사용자가 늘어 자체 서버(Vapor)로 갈 때를 위한 출구.
- **실시간:** Firestore 리스너(피드·신호 상태) + APNs(Live Activity 갱신). 프레즌스("지금 라운지 12명")는 v2로 보류 — 감시당하는 느낌과 한 끗 차이라 신중하게.
- **인증:** 아카데미 이메일 도메인 화이트리스트 + Sign in with Apple 연동.
- **클라이언트:** SwiftUI 100%, Swift Concurrency, MVVM+@Observable, SPM 멀티모듈(App / Core / DesignSystem / GyeopKit(UWB·MPC) / SignalKit / GraphKit), Swift Testing, TestFlight+GitHub Actions.

## 7. 빌드 순서 (개인 프로젝트 스프린트)

| 스프린트 | 산출물 | 데모 가능한 것 |
|---|---|---|
| S1 (1주) | F1 카드 + 모듈 골격 | 카드 생성, 시드 재현 |
| S2 (1주) | F2 맞대기 (MPC → UWB) | 실기기 2대 교환 + 겹 기록 |
| S3 (2주) | F3 신호 + 서버 + 푸시 + Live Activity | 잠금화면 실시간 갱신 |
| S4 (1주) | F7 위젯·D-day + F8 App Intents | 앱 안 열고 신호 켜기 |
| S5 (2주) | F5 룰렛 + F4 그래프 | 월요일 매칭 푸시, 관계 지도 |
| S6 (1주) | F6 스팟·스탬프 | 위치 인증 스탬프 |
| S7 | TestFlight 러너 배포 → 운영 사이클 | 실사용 지표 |

S3 종료 시점(4주 차)에 이미 "쓸 수 있는 앱"이다. S4 이후는 전부 리텐션과 차별화.

## 8. 성공 지표 (문제 정의와 1:1 대응)

- 손실 1(우연) → **팀 밖 겹 비율**: 전체 겹 중 팀 밖 비중 30% 이상
- 손실 2(제안 비용) → **신호 성사율** 60% 이상, **룰렛 성사율** 40% 이상
- 손실 3(유한함) → **월간 활성 겹 러너**: 설치자 중 월 1겹 이상 60%, 수료까지 유지

체류시간·DAU는 지표로 삼지 않는다. 오프라인에서 일어난 일만 센다.

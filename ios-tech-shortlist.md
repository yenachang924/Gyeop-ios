# iOS 기술 쇼트리스트 — "구현하면 할 얘기 많은" 것들

`runner-app-plan.md`의 기능 목록을 전제로, 어떤 기술에 깊이를 투자해야 면접·서류·블로그에서 이야기가 쏟아지는지를 티어로 정리한다.

판정 기준은 세 가지다. 셋 다 만족해야 S티어.

1. **스토리 밀도** — 문제 → 결정(트레이드오프) → 검증의 3막 구조가 나오는가. "써봤다"로 끝나는 기술은 탈락.
2. **데모 임팩트** — 시연 10초 안에 "오" 소리가 나는가.
3. **핵심 루프와의 겹침** — 앱의 P1 기능과 겹치는가. 앱과 무관한 기술 자랑은 짐이다.

참고 자료: 디프만 15기 서류·면접 후기, 디프만 iOS 14기 면접 질문 리스트(GCD, Class/Struct, OOP/POP, RxSwift), SeSAC 3기 후기, 그리고 kimscastle(아카데미 1기·SOPT 32기)의 "출시 후 개발일지" — 특히 마지막 글은 **출시 이후의 에러 처리·유저 피드백·"왜"를 설명 못 하는 코드**가 어떻게 12시간짜리 사고가 되는지를 보여주는 반면교사다.

---

## S티어 — 반드시 구현 (P1 기능과 일치)

### S1. NearbyInteraction(UWB) + MultipeerConnectivity — 카드 맞대기 (F2)

**할 얘기:**
- Discovery token 교환 핸드셰이크: MPC로 피어를 찾고, NI 세션으로 승격하는 2단 설계
- 거리·방향 이벤트를 `AsyncStream`으로 감싸 뷰모델에서 구조화 동시성으로 소비한 경험
- 교환 프로토콜의 상태 머신 설계 (발견 → 승인 → 교환 → 확인, 각 단계의 타임아웃과 실패 복구)
- U1 칩 미지원 기기(iPhone X 이하)·권한 거부 시 MPC-only 폴백을 1급 시민으로 설계한 이유
- 백그라운드 제약(NI는 포그라운드 전용)을 UX로 풀어낸 방법
- **면접 직결:** P2P 네트워킹, 비동기 스트림, 프로토콜 설계, 하드웨어 파편화 대응

**함정:** 시뮬레이터에서 NI 테스트 불가 → 실기기 2대 필요. MPC 폴백부터 만들 것.
**데모 임팩트:** 최상. 심사위원 폰 두 대로 그 자리에서 시연 가능.

### S2. ActivityKit(Live Activity·Dynamic Island) + APNs 푸시 갱신 — 여가 신호 (F3)

**할 얘기:**
- Live Activity를 로컬 갱신이 아니라 **서버 푸시로 갱신**하는 파이프라인 전체: p8 token 기반 APNs 인증, push token 수명 관리, payload 설계
- stale date·종료 정책으로 휘발성 신호의 수명을 관리한 방법 (서버 TTL과 클라이언트 타이머 이중화)
- Dynamic Island의 제약(업데이트 빈도, 콘텐츠 크기, 레이아웃 4종) 안에서 정보 설계
- 앱이 죽어 있어도 잠금화면에서 신호가 살아 있게 만드는 것 = "오프라인 제약 돌파"의 기술적 증명
- **면접 직결:** 푸시 아키텍처, 서버-클라이언트 상태 동기화, 제약 조건 하의 설계

**함정:** Live Activity 푸시는 실기기+운영 APNs 환경에서만 온전히 검증됨. 로컬 갱신 → 푸시 갱신 순서로.
**데모 임팩트:** 상. 잠금화면·Dynamic Island가 갱신되는 순간이 곧 시연.

### S3. Swift Concurrency 심화 + 에러 처리·관측 가능성 — 전 기능 관통

kimscastle 글의 교훈을 그대로 뒤집어 설계로 만든다: 그는 `withThrowingTaskGroup` 병렬 업로드에서 **구현부 do-catch가 모든 에러를 한 덩어리로 뭉개버려** 원인 파악에만 반나절을 썼다. 우리는 이걸 처음부터 아키텍처로 막는다.

**할 얘기:**
- 에러를 잡는 위치의 원칙: 구현부는 던지기만, 최종 호출부(뷰모델/유스케이스)에서만 catch — 그리고 왜
- 도메인 에러 계층 설계 (`enum AppError` + 원인 보존 `underlying error`), Swift 6 typed throws 적용 검토
- `TaskGroup` 병렬 작업(카드 이미지 프리페치 등)에서 부분 실패 정책 — 전체 실패 vs 성공한 것만 수집
- actor로 신호 상태 캐시 격리, Swift 6 strict concurrency(Sendable) 대응기
- `OSLog` 구조화 로깅 + 원격 로깅(Crashlytics)으로 "유저 폰에서 무슨 일이 났는지"를 출시 첫날부터 아는 상태 만들기
- **면접 직결:** GCD vs 구조화 동시성(단골 질문의 실전 답), 에러 처리 철학, 운영 경험

**함정:** 과잉 설계 유혹. 에러 계층은 화면에 보여줄 구분(재시도 가능/불가/로그인 필요)만큼만.
**데모 임팩트:** 하 — 하지만 서류·블로그·면접에서 가장 어른스러운 이야기가 나오는 축.

---

## A티어 — 구현하면 좋음 (P2, 가성비 순)

### A1. App Intents — 신호 토글을 시스템에 심기
"시리야, 보드게임 신호 켜줘" + 잠금화면 버튼 + 단축어. 구현량 대비 데모 임팩트가 S급이고, 아직 구현해 본 지원자가 드물어 신선하다. `AppIntent` 파라미터 설계, 앱 프로세스 밖 실행 얘기가 나온다. **가성비 최고 — S3까지 끝나면 1순위로 추가.**

### A2. WidgetKit — 겹 위젯
타임라인 예산(하루 40~70회 갱신)과 예측 갱신 설계, App Group으로 앱-위젯 데이터 공유, 잠금화면 액세서리 위젯. Live Activity(S2)와 코드 기반이 겹쳐 한계비용이 낮다.

### A3. SwiftData — 겹 기록 저장
`@Model` 관계 모델링(러너-만남-스팟), 마이그레이션 플랜, 백그라운드 컨텍스트 동기화. CoreData 대비 무엇이 좋아지고 무엇이 아직 아쉬운지(쿼리 표현력)를 말할 수 있으면 충분.

### A4. MeshGradient + Metal `layerEffect` — 정체성 카드 비주얼
관심사 시드 → 결정적(deterministic) 비주얼 생성 알고리즘. SwiftUI 렌더링 파이프라인과 셰이더가 만나는 지점이라 "SwiftUI 어떻게 그려지나" 질문의 고급 답변이 된다. 단, 여기 빠지면 2주가 사라진다 — 시간 박스 필수.

### A5. MapKit — 포항 스팟 맵
SwiftUI MapKit API, 어노테이션 클러스터링, 공공데이터 정규화. 스토리 밀도는 중간이지만 앱 완성도에 필수.

### A6. PhotosKit(PHAsset) — 프로필/모임 사진 (선택 기능 넣을 경우만)
kimscastle이 당한 iCloud 동기화 함정(`isNetworkAccessAllowed`)이 그 자체로 스토리. 다만 우리 앱 핵심 루프에 사진이 필수는 아니므로, 기능이 생길 때만.

---

## B티어 — 구현 대상이 아니라 기본기 (공식문서로 읽고, 앱에서 겪은 지점에 연결)

이 항목들은 "기능"이 아니다. 하지만 면접 단골이고, 위 구현 과정에서 자연스럽게 마주치므로 **공식문서 → 내 코드의 사례** 순으로 연결해 두면 답변이 산다.

| 주제 | 우리 앱에서 마주치는 지점 |
|---|---|
| 앱/씬 생명주기 | NI 세션이 백그라운드 진입 시 끊기는 순간의 처리 |
| ARC·메모리 | AsyncStream continuation·클로저 캡처에서의 순환 참조 |
| Class vs Struct·값 의미론 | 카드 = 불변 스냅샷 struct로 설계한 이유 |
| GCD | "왜 안 썼는가"를 설명하기 위해 알아야 함 |
| OOP vs POP | Endpoint 프로토콜·리포지토리 추상화 |
| Combine/RxSwift | Observation+AsyncSequence로 대체한 근거와 한계 |
| HIG | Dynamic Island·위젯의 콘텐츠 가이드 준수 |

참고: 함께 보던 티스토리 "영문 공식문서 뜯어보기" 시리즈는 현재 작업 환경에서 접근이 차단되어 글 목록을 직접 대조하지 못했다. 시리즈의 주제 목록을 주면 위 표에 1:1로 매핑해 업데이트한다.

---

## 최종 추천 — 투자 순서

1. **S1 카드 맞대기 (UWB+MPC)** — 차별화의 몸통. 남들이 못 흉내 냄.
2. **S2 Live Activity 푸시 갱신** — 서버 포함 풀스택 스토리. "배포는 시작"의 증거.
3. **S3 동시성+에러 처리+로깅** — 기능이 아니라 체질. 서류의 어른스러움은 여기서 나옴.
4. **A1 App Intents** — 여력 생기면 즉시. 구현 3일, 임팩트 S급.

이 4개면 디프만 iOS 14기 면접 질문 리스트(GCD, Class/Struct, OOP/POP, Rx 대체재)의 전 항목에 실전 사례로 답할 수 있고, 회고 블로그 3편(UWB 교환기 / Live Activity 푸시 / 에러 처리 아키텍처)이 그대로 나온다.

하지 말 것: CoreML, ARKit, visionOS 같은 "멋있어 보이는" 기술을 핵심 루프와 무관하게 얹는 것. 기준 3(핵심 루프와의 겹침)에서 전부 탈락이다.
